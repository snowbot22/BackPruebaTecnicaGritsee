/*
package com.example.testgritsee.data;

import jakarta.persistence.Id;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;

import java.util.List;

public interface DataRepository extends JpaRepository<Data, Integer>, JpaSpecificationExecutor<Data> {
    //Filtro de sucursal especifica dentro de url
    List<Data> findByUrlContainingIgnoreCase(String sucursal);
}
*/